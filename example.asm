# Загружаем значения в память
LOAD_CONST 256         # Загружаем 256
WRITE_MEM 0            # Записываем в память по адресу 0

LOAD_CONST -100        # Загружаем -100
WRITE_MEM 1            # Записываем в память по адресу 1

LOAD_CONST 0           # Загружаем 0
WRITE_MEM 2            # Записываем в память по адресу 2

LOAD_CONST 150         # Загружаем 150
WRITE_MEM 3            # Записываем в память по адресу 3

# Читаем значения из памяти, применяем SGN и записываем результат обратно
READ_MEM 0             # Читаем значение из памяти по адресу 0
SGN                    # Применяем sgn()
WRITE_MEM 0            # Записываем результат в память по адресу 0

READ_MEM 1             # Читаем значение из памяти по адресу 1
SGN                    # Применяем sgn()
WRITE_MEM 1            # Записываем результат в память по адресу 1

READ_MEM 2             # Читаем значение из памяти по адресу 2
SGN                    # Применяем sgn()
WRITE_MEM 2            # Записываем результат в память по адресу 2

READ_MEM 3             # Читаем значение из памяти по адресу 3
SGN                    # Применяем sgn()
WRITE_MEM 3            # Записываем результат в память по адресу 3
